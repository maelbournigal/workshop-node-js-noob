exports.run = function(input) {
  var separation = input.split(" , ");
  for( var i = 0; i<separation.lenght; i++){
    var test = 0;
    if(separation[i]>test){test = separation[i]};
  }
  console.log(test);
};
